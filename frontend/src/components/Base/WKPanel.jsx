import { Box } from '@mui/material';
import { styled } from '@mui/system';

const WKPanel = styled(Box)({
    padding: '16px',
    border: '1px solid rgba(0, 0, 0, 0.1)',
    borderRadius: '20px',
    display: 'flex',
    alignItems: 'center'
});

export default WKPanel;